version: 2
updates:
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      dev-dependencies:
        dependency-type: development
    ignore:
      # tap 14 -> 21 is a breaking migration (drops t.deepEqual/deepEquals/
      # strictDeepEquals/equals, changes snapshots). Hold the major until the
      # test suite is migrated; minor/patch bumps are still allowed.
      - dependency-name: "tap"
        update-types: ["version-update:semver-major"]

  # The wasm-bindgen bump (issue #11) is exactly what this should keep current:
  # track the Rust crates so the glue doesn't rot on old versions again.
  - package-ecosystem: cargo
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
